machine:
  php:
    version: 5.6.14
  environment:
    DB_HOST: 127.0.0.1
    DB_USERNAME: ubuntu
    DB_DATABASE: circle_test

dependencies:
  post:
      - chmod -R 777 ~/larabor/storage
      - php artisan env